# Contract — F<ID> <Feature Name>

> Operational contract for this feature. Generated by `spec-writer` from the PRD + spec.
> Consumed by `implement-feature` (must satisfy it) and by `evaluator` (validates against
> it). Base doc: `../../docs/Contrato_de_Feature.md`.

## Environment Contract

Minimum prerequisites for the feature to be executed and tested. If the environment does not
meet this, evaluation does NOT start (environment-invalid ≠ implementation-wrong).

- <e.g. Dev runtime of the web app active, serving route `/`>
- <e.g. Real browser via `Playwright CLI`>
- <complex projects: messaging / brokers / microservices / database>

## Quality Gates

Objective technical floor of acceptance, run before functional inspection. Discover the
actual commands from the project (`package.json` scripts, Makefile, etc.). See
`../../docs/Como_criar_gates.md`.

- [ ] <gate-id: typecheck> — e.g. `tsc --noEmit`
- [ ] <gate-id: lint> — e.g. `eslint . --max-warnings=0`
- [ ] <gate-id: build> — e.g. project build
- [ ] <gate-id: tests> — e.g. unit/integration test command
- [ ] <gate-id: arch> — e.g. dependency-cruiser / custom architecture check (when present)

> Each gate has a stable `id` (referenced by the evaluation-report `failures[].ref`).

## Coverage Manifest

Maps expected behaviors to validation surfaces (each surface has a stable ID).

- Surface `<ID e.g. Public-01>` (<UI · public access | API | CLI | ...>) → covers <scope>

## Surfaces & Behaviors

### Surface: `<ID>` — <description>
- Initial state: <e.g. anonymous visitor, no cookies, no authenticated session, on `/`>
- Behaviors:
  - [ ] <behavior 1 — observable>
  - [ ] <behavior 2 — observable>

## Observable Criteria

Each acceptance is an observable signal with verifiable evidence (not a vague description).

- [ ] `<crit-id>` — <observable criterion, e.g. registration CTA present>
- [ ] `<crit-id>` — <observable criterion, e.g. login in top navigation>

> Each criterion has a stable `id` (referenced by the evaluation-report `failures[].ref`).
